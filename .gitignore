# docs/openapi.json is a committed symlink to ../openapi.json:
# GitHub Pages serves the docs/ folder and needs the spec inside it.
.env
schemathesis-report/
# Local test-tool caches: contents churn (timestamps, generated examples) on every run.
.schemathesis/
.hypothesis/
.ocli