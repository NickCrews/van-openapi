<!-- Generated by `make agent-docs` from openapi.json, transcripts/ and STATUS.md. Do not edit by hand. -->

# listNoteCategories — GET /notes/categories

List note categories

## Verified behaviors

- **Note categories come back as a bare array that ignores paging** — The list is a bare JSON array, not the pagination envelope, and query parameters like `$top` are ignored rather than applied or rejected. The sandbox committee has no categories opted in, so the empty array is all it can answer. ([replay](../behaviors/note-categories-are-a-bare-array.md))

## Behavior

Returns every note category available in the current context as a **bare JSON array** — this endpoint does not use the pagination envelope, and it ignores query parameters such as `$top`.

A category only appears here once it has been opted in to the current context's committee through the Committees picker on its detail page in the VAN UI; being listed on the Note Categories page is not enough. The sandbox committee has none opted in, so only the empty array has been observed live.

Only a category whose `assignableTypes` contain `Person` can be used on a person note; sending any other id to `POST /people/{vanId}/notes` returns 400. See `GET /notes/categoryTypes` for the full vocabulary of `assignableTypes`.

## Example

```bash
curl -sS -X GET \
  'https://api.securevan.com/v4/notes/categories' \
  -u "app-name:$NGP_API_KEY|1"
```

## Responses

| Status | Meaning | Body |
| --- | --- | --- |
| `200` | All note categories in the context, as a bare array. | array of object |
| `400` | Validation error, as the standard JSON envelope. ([details](../ERRORS.md#badrequest)) | [ErrorResponse](../ERRORS.md#errorresponse) |
| `401` | Missing/invalid API key, or a database mode the key does not have access to. ([details](../ERRORS.md#unauthorized)) | [ErrorResponse](../ERRORS.md#errorresponse) |
| `404` | The request never reached the API. ([details](../ERRORS.md#querystringtoolong)) | _empty_ |
| `429` | Rate limit exceeded. ([details](../ERRORS.md#toomanyrequests)) | [ErrorResponse](../ERRORS.md#errorresponse) |
| `500` | Internal server error. ([details](../ERRORS.md#servererror)) | [ErrorResponse](../ERRORS.md#errorresponse) or [AspNetError](../ERRORS.md#aspneterror) |
