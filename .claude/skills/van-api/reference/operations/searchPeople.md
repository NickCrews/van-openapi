<!-- Generated by `make agent-docs` from openapi.json, transcripts/ and STATUS.md. Do not edit by hand. -->

# searchPeople — GET /people

Search for people

## Verified behaviors

- **Text criteria match case-insensitive prefixes, never substrings** — Matching runs from the start of the field, ignoring case: `wilhel` finds Wilhelmina, `helmina` finds nothing. Hits come back as full person records in the pagination envelope, with collections null unless $expand asks for them. ([replay](../behaviors/search-is-a-case-insensitive-prefix-match.md))
- **Only a bare 10-digit phone number finds anyone** — The comparison is against the stored 10-digit string, so `(907) 555-0137` is accepted as valid input and then silently matches no one. There is no error to catch — a formatted number just looks like a person who is not there. ([replay](../behaviors/search-phone-must-be-bare-digits.md))
- **At least one search parameter must be set, and filters don't count** — A request with no search parameter is refused outright — even one that sets paging or filter parameters. `contactMode` is a filter, not a search parameter, so it alone does not satisfy the requirement either. ([replay](../behaviors/search-requires-a-search-parameter.md))

## Behavior

Searches for people matching the given criteria and returns full person records in the standard pagination envelope. At least one search parameter must be set: firstName, lastName, middleName, streetAddress, city, stateOrProvince, zipOrPostalCode, phoneNumber, email, commonName, or officialName. A request with none of these — even one with filter or paging parameters set — returns 400 INVALID_PARAMETER with the text "This endpoint requires at least one search parameter to be set." Text criteria are case-insensitive prefix matches: lastName=Testpers matches "Testperson", lastName=estperson does not. Related collections on the returned person records are null unless requested via $expand.

**This is the endpoint to use when you only have loose criteria.** Unlike POST /people/find and POST /people/findOrCreate — which refuse to attempt a match until you supply one of a handful of minimum field combinations, and answer Unmatched (or create a duplicate) when you cannot — this search has no such threshold. A single name is a perfectly valid query. In exchange it makes no identity claim: it hands back every person whose fields start with what you typed, and deciding which one is your person, or that none of them is, is your job. Inspect the candidates (with $expand for addresses, emails and phones if you need them) and take the vanId from there.

## Example

```bash
curl -sS -X GET \
  'https://api.securevan.com/v4/people?firstName=Ada' \
  -u "app-name:$NGP_API_KEY|1"
```

## Query parameters

| Name | Type | Notes |
| --- | --- | --- |
| `firstName` | string | First name. Case-insensitive prefix match. |
| `lastName` | string | Last name. Case-insensitive prefix match. |
| `middleName` | string | Middle name. |
| `streetAddress` | string | Street address, matched as a case-insensitive prefix — but only against the person's **Voting (home) address**, as are `city`, `stateOrProvince` and `zipOrPostalCode`. All four read the single address VAN files as Voting; Work and Custom addresses are not indexed at all, and a Mailing address is only reachable when the person has no separate Voting address (VAN then serves the mailing one under both types, and it becomes findable). So a person can hold an address and still not be findable by it. The comparison also runs against the *standardized* form VAN stored rather than the text that was written: a person created with `1000 W 5th Ave` is found by `1000 5Th Ave Mall` and **not** by `1000 W 5th Ave`. POST /people/find has neither limitation — it matches every address type, including the Work and Custom addresses that $expand=addresses never shows. |
| `city` | string | City, matched as a case-insensitive prefix (`anchor` finds Anchorage; `nchorage` finds nothing). Like `streetAddress`, it reads the **Voting (home) address only** — a person whose mailing or work address sits in a different city is not returned for that city — and it compares against the city VAN standardized at write time, which is not always the one that was written. |
| `stateOrProvince` | string | State or province, as the two/three character code — case-insensitive but **not** a prefix match and not the spelled-out name: `AK` and `ak` match, `A` and `Alaska` return nothing. Like `streetAddress`, it reads the **Voting (home) address only**. |
| `zipOrPostalCode` | string | ZIP or postal code. Like `streetAddress`, this matches the **Voting (home) address only** — a person whose mailing address sits in a different ZIP is not returned for that ZIP. The value is validated as a postal code (`995` and `995012357` both return 400 INVALID_PARAMETER "is not a valid Postal Code for UsaAndInternational"), and only the 5-digit form matches: a person whose stored ZIP is `99501-2357` is found by `99501` and not by `99501-2357`, which is accepted but returns nothing. |
| `phoneNumber` | string | Phone number. **Send bare 10 digits — `9072223333`.** Anything else silently finds nothing: the comparison is against the stored 10-digit string, so `(907) 222-3333`, `907-222-3333` and the `1`-prefixed `19072223333` are all *accepted* and then match no one. (Validation is laxer than matching: non-numeric characters are stripped before the value is checked against `^1?[2-9][0-8]\d[2-9]\d{6,6}$`, which is why the formatted forms get through. Failing that regex returns 400 INVALID_PARAMETER with it in the hint.) This checks **one number per person**: the number VAN flags `isBest` (the first entry `$expand=phones` returns). A person holding home, work, cell and fax numbers is findable only by whichever one is best — for that person the cell — even though the others are on the person record and POST /people/find matches them. A person can also have **no** best number — when every number on it is a work, other or fax number and none was written `isPreferred` — and such a person cannot be found through this parameter at all. See `PersonPhone.isBest` for how the best number is picked. |
| `email` | string | Email address. Case-insensitive prefix match, checked against **all** of the person's email addresses, not just the preferred one. Prefix means from the start of the address: `probe.charlie` finds `probe.charlie@example.com`, `charlie@example.com` does not. |
| `commonName` | string | Organization common name. |
| `officialName` | string | Organization official name. |
| `contactMode` | string | Filters the results to Person or Organization contacts. Does not count toward the at-least-one-search-parameter requirement, and any other value is ignored rather than rejected. One of: `Person`, `Organization`. |
| `employer` | string | Accepted but has no effect on the results, and does not count toward the at-least-one-search-parameter requirement. |
| `occupation` | string | Accepted but has no effect on the results, and does not count toward the at-least-one-search-parameter requirement. |
| `dateOfBirth` | string | Accepted but has no effect on the results, and does not count toward the at-least-one-search-parameter requirement. Even an unparseable value is accepted and ignored. |
| `$top` | integer (int32) | Limits the number of results per page. Default 50, maximum 200; 0 and negative values return 400. |
| `$skip` | integer (int32) | Number of results to skip. Negative values return 400. |
| `$orderby` | string | Sort expression. The only sortable property is Name; "Name desc" sorts descending. Any other property returns 400 INVALID_PARAMETER with the hint "The only valid orderby property is: 'Name'". |
| `$expand` | array of string | Related sections to include, comma-delimited and case-insensitive eg 'addresses,emails' — a much smaller set than GET /people/{vanId} accepts. The API's hint also lists pollingLocation, but requesting it is then refused with 400 BAD_REQUEST "May not request 'pollingLocation' in this context", alone or combined, so the usable set is the four below. Anything outside the hinted set returns 400 INVALID_PARAMETER. Expanded collections come back as empty arrays when absent; unexpanded sections stay null. One of: `addresses`, `emails`, `phones`, `districts`. |

`*` = required.

## Responses

| Status | Meaning | Body |
| --- | --- | --- |
| `200` | A page of matching people. | `PersonList` |
| `400` | Validation error. Most are INVALID_PARAMETER and name the offending query parameter in `properties`; the no-search-parameter case names the pseudo-property "All Arguments", and refusing an out-of-context $expand section uses code BAD_REQUEST with no `properties` or `hint` at all. | [ErrorResponse](../ERRORS.md#errorresponse) |
| `401` | Missing/invalid API key, or a database mode the key does not have access to. ([details](../ERRORS.md#unauthorized)) | [ErrorResponse](../ERRORS.md#errorresponse) |
| `404` | The request never reached the API. ([details](../ERRORS.md#querystringtoolong)) | _empty_ |
| `429` | Rate limit exceeded. ([details](../ERRORS.md#toomanyrequests)) | [ErrorResponse](../ERRORS.md#errorresponse) |
| `500` | Internal server error. ([details](../ERRORS.md#servererror)) | [ErrorResponse](../ERRORS.md#errorresponse) or [AspNetError](../ERRORS.md#aspneterror) |

### `200` body — PersonList

The envelope every paginated endpoint returns around its `items` array.

| Property | Type | Notes |
| --- | --- | --- |
| `nextPageLink` | string (uri) or null | Absolute URL of the next page — including an explicit `:443` port — or null on the last page. e.g. `https://api.securevan.com:443/v4/people?lastName=Testperson&$top=1&$skip=1` |
| `count` * | integer | Total number of matches across all pages, not the size of this page. |
| `items` * | array of [Person](../schemas/Person.md) |  |

`*` = required.

## What to call next

- [`getPerson`](getPerson.md) — Fetch the first matching person's full record (e.g. with the larger $expand set that getPerson accepts).
