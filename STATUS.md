# Endpoint Survey

Part of [NickCrews/van-openapi](https://github.com/NickCrews/van-openapi).

Research status of NGP VAN API endpoints. Base URL: `https://api.securevan.com/v4`.

**Statuses:** `unresearched` → `researched` (docs read + sandbox probed) → `spec'd` (in openapi.json) → `validated` (passing `make fuzz`).

Sources per endpoint group:
- **docs** — https://docs.ngpvan.com (index of pages: https://docs.ngpvan.com/llms.txt)
- **van-cli** — https://github.com/NGPVAN/van-cli `src/commands/`

> Sandbox note: the sandbox key is **My Campaign (mode `|1`) only** (`hasMyVoters: false`, "Demo State" committee). Endpoints requiring My Voters mode cannot be probed with it.

| Endpoint group | Known paths | Sources | Status | Last researched |
|---|---|---|---|---|
| Echoes | `POST /echoes` | docs | **validated** | 2026-08-03 |
| People — find | `POST /people/find` | docs, van-cli | **validated** (incl. minimum match combinations) | 2026-08-04 |
| Survey Questions — list | `GET /surveyQuestions` | docs, van-cli | **validated** | 2026-08-03 |
| People — get by id | `GET /people/{vanId}` | docs, van-cli | **validated** (incl. stateful links from `/people/find`) | 2026-08-04 |
| People — write | `POST /people/create`, `POST /people/findOrCreate`, `POST /people/{vanId}` (update) | docs (common models only), van-cli | **validated** | 2026-08-04 |
| People — search | `GET /people` (requires ≥1 search param; `GET /people/quickSearch` does **not** exist — 404 "No HTTP resource") | van-cli | **validated** | 2026-08-04 |
| People — delete | `DELETE /people/{vanId}` | van-cli | researched (route exists; sandbox key gets `403 FORBIDDEN` "Access to this action is restricted", so success shape unknown — deliberately left out of the spec) | 2026-08-03 |
| People — notes | `GET/POST /people/{vanId}/notes`, `GET/PUT/DELETE /people/{vanId}/notes/{noteId}` | docs, van-cli | **validated** (list + create; `PUT` spec'd from docs only and `GET`/`DELETE` by id left out — all three are `403 FORBIDDEN` "Access to this action is restricted" for the sandbox key) | 2026-08-04 |
| People — sub-resources | `/people/{vanId}/{activistCodes,canvassResponses,customFields,scores,stories}` | van-cli | unresearched | — |
| Survey Questions — CRUD | `POST /surveyQuestions`, `GET/PUT /surveyQuestions/{id}` | docs, van-cli | researched (create 400s captured; valid `type` values unknown) | 2026-08-03 |
| API Key Profiles | `GET /apiKeyProfiles` | van-cli | researched (probed; returns items envelope with rate-limit profile) | 2026-08-03 |
| Activist Codes | `GET /activistCodes`, `GET /activistCodes/{id}` | docs, van-cli | unresearched | — |
| Bulk Import | `GET/POST /bulkImportJobs`, `/{jobId}/{cancel,errors,results,start,upload}` | docs, van-cli | unresearched | — |
| Canvass Responses (lookups) | `GET /canvassResponses/{contactTypes,inputTypes,resultCodes}` | van-cli | researched (probed contactTypes: bare array, no envelope) | 2026-08-03 |
| Changed Entity Export Jobs | `POST /changedEntityExportJobs`, `/{jobId}`, `/{jobId}/{cancel,downloadUrl,status}` | van-cli | unresearched | — |
| Codes | `GET /codes/{contactTypes,inputTypes,resultCodes,supporterGroups}` + by-id | van-cli | unresearched | — |
| Contributions | `GET/POST /contributions`, `/{id}`, `/recentContributions`, `/payments` | docs, van-cli | unresearched | — |
| Custom Fields | `GET /customFields`, `/{id}` | van-cli | unresearched | — |
| Departments | `GET /departments`, `/{id}` | docs | unresearched | — |
| Designations | `GET /designations`, `/{id}` | docs (changelog), van-cli | unresearched | — |
| Disbursements | `GET /disbursements/recentDisbursements` | docs | unresearched | — |
| Early Vote Fields | `GET /earlyVoteFields`, `/{id}` | docs | unresearched | — |
| Email | `GET /email/messages`, `/email/message/{foreignMessageId}` | van-cli | unresearched | — |
| Employers / Labor | `/employers/{id}/bargainingUnits/…`, `/jobClasses/{id}`, `/worksites/{id}/…` | docs | unresearched | — |
| Events | `GET/POST /events`, `/{eventId}`, `/events/types`, `/types/{id}` | docs, van-cli | unresearched | — |
| Export Jobs | `GET/POST /exportJobs`, `/{id}`, `/{id}/downloadUrl` | van-cli | unresearched | — |
| File Loading Jobs | `POST /fileLoadingJobs` | docs | unresearched | — |
| Folders / Map Regions | `/folders/{folderId}/mapRegions`, `/{mapRegionId}`, `…/refresh` | docs | unresearched | — |
| Locations | `GET/POST /locations`, `/{id}`, `/findOrCreate` | van-cli | unresearched | — |
| MiniVAN Exports | `GET /minivanExports` | docs | unresearched | — |
| Note Categories | `GET /notes/categories` | docs, van-cli | **validated** (bare array, no envelope; sandbox committee has none opted in, so only the empty array observed — item shape from docs) | 2026-08-04 |
| Printed Lists | `GET /printedLists` | docs | unresearched | — |
| Saved Lists | `GET /savedLists`, `/{id}` | van-cli | unresearched | — |
| Scores | `GET /scores`, `/{id}`, `/{id}/committeeAccess` | docs, van-cli | unresearched | — |
| Signups | `GET/POST /signups`, `/{id}` | van-cli | unresearched | — |
| Stories | `GET/POST /stories`, `/{id}` | van-cli | unresearched | — |
| Supporter Groups | `GET/POST /supporterGroups`, `/{id}`, `/{id}/people/{vanId}` | van-cli | unresearched | — |
| Targets | `GET /targets/{id}`, `/targets/subgroups` | van-cli | unresearched | — |
| Targeted Emails | (paths TBD — see van-cli `targetedEmails.ts`) | van-cli | unresearched | — |
| Voter Registration Batches | `POST /voterRegistrationBatches/{batchId}/people` | docs | unresearched | — |
| Introspection | (see docs "Introspection" page) | docs | unresearched | — |

## Field notes (from sandbox probing, 2026-08-03)

- **Auth**: HTTP Basic. Username = application name (anything works in sandbox; van-cli defaults to `default_user`), password = `{apiKey}|{databaseMode}`. Mode `0` = My Voters, `1` = My Campaign. Wrong mode → `401 {"errors":[{"code":"UNAUTHORIZED","text":"Invalid Database Mode"}]}`.
- **Error envelope**: `{"errors":[{"code","text","properties":[…],"hint"?}]}`. Seen codes: `UNAUTHORIZED`, `INVALID_PARAMETER`, `BAD_REQUEST`, `NOT_FOUND`, `429`.
- **Rate limiting (429)**: exceeding the key's rate-limit profile returns the standard error envelope with `code` the numeric string `"429"` (the only non-symbolic code seen) and text `"Rate limit exceeded. Try again in N ms."` — there is **no `Retry-After` header** (225 429s captured 2026-08-04, none carried one; the retry hint exists only in the text). Per-key, so any endpoint can return it: first surfaced by the fuzzer on `POST /people/create`, reproduced with ~300 concurrent `GET /surveyQuestions`. Spec'd as the shared `TooManyRequests` response on every operation. Rate limiting is absent from the official docs entirely (`x-docs-discrepancy` on the component).
- **Pagination envelope**: `{"items":[…],"nextPageLink":null|string,"count":int}` — but not universal: `/canvassResponses/contactTypes` returns a bare array.
- **`$top`**: default 50, max 200 per the `INVALID_PARAMETER` hint on `/surveyQuestions`.
- **`POST /people/find` does not behave as docs imply**: match → `302` with `Location: /v4/people/{vanId}` and body `{"vanId":int,"status":"Matched"}`; no match → `404` with `{"vanId":null,"status":"Unmatched"}` (even for sparse criteria like firstName-only — no 400 observed). `findOrCreate` → `201 {"vanId":int,"status":"UnmatchedStored"}`.
- **Matching has a minimum-criteria threshold** (docs' Match Candidate model, https://docs.ngpvan.com/reference/common-models; every combination confirmed live 2026-08-04 against a purpose-built sandbox record, vanId 102719047 — `Match Threshold`, dob 1980-04-15, `match.threshold@example.com`, `9075554242`, 742 Evergreen Terrace / 99501). The matcher does nothing until the criteria reach one of: name+email, name+phone, name+zip5+dob, name+street number+street name+zip5, or email alone. Observed:
  - **Unmatched (404) even though the person exists**: `lastName` alone; `firstName+lastName`; `firstName+lastName+dateOfBirth`; `firstName+lastName+zip`; street name without the street number + zip; right street + wrong zip; wrong `firstName` + right email (criteria only ever narrow).
  - **Matched (302)**: email alone; **phone alone** (a discrepancy — the docs list only email as sufficient on its own); name+email; name+phone; name+dob+zip; name+street+zip. Comparison is case-insensitive on names and emails, and ignores phone punctuation (`907-555-4242` = `9075554242`).
  - **`addresses` is accepted as find criteria** and is the only way to supply zip5 or a street — it was missing from `PersonMatchCriteria`, now spec'd as `PersonMatchAddress`. Unlike `phones`, `addresses: [null]` is tolerated (404, not 500).
  - **On `findOrCreate` the threshold is a duplicate factory**: `{"firstName":"Match","lastName":"Threshold"}` for the existing 102719047 returned `201 UnmatchedStored` with a new vanId (102719048). Below-threshold callers cannot tell "no match" from "not enough info", so the spec tells them to fall back to `GET /people` (no threshold, loose prefix search) and pick the record themselves.
  - **Ambiguous matches are resolved silently** (probed 2026-08-04 with three deliberately identical records — 102719047, 102719048, 102720144, same name/email/phone): exactly one vanId comes back with nothing in the response signalling that others matched, so a `302` is not proof of uniqueness. The tiebreak is deterministic and depends on the criteria shape — **criteria including a name always returned the lowest vanId** (oldest record), **name-less criteria (email alone, phone alone) always the highest** (newest). Ruled out: recency of modification (touching either record moved nothing) and best-match quality (enriching the newer duplicate with the same dob and address did not move a name-based match off the older one). `findOrCreate` matched the same way (302, no create). Practical risk: an update through `POST /people/{vanId}` lands on whichever duplicate won the tiebreak while the other keeps drifting.
- **`POST /echoes`** accepts an empty body; all fields nullable in response (`{"message","dateSent","delayInMilliseconds"}`). **BUG**: `delayInMilliseconds: -1` hangs the connection indefinitely (likely passed straight into .NET as `Timeout.Infinite`); other negative values return 500.
- **Fuzzer-found 500s** (should be 400s): negative `delayInMilliseconds` on `/echoes`; non-ASCII filter values on `/surveyQuestions`; `vanId: 0` or `phones: [null]` on `/people/find`. Documented as 500 responses in the spec; `not_a_server_error` check disabled in `make fuzz`.
- **Redirect-following clients** (requests, fetch, httpx, schemathesis) see `POST /people/find` return **200 with the full person record** because they auto-follow the 302 to `GET /people/{vanId}`, downgrading POST to GET on the way. Same for the other three redirecting writes — `findOrCreate`/`create` on a match and `POST /people/{vanId}` (update), where the followed GET is the only way to see the merged record. The spec documents both the raw 302 and the followed 200 on all four, each with the full request/response chain in the 200's description (verified end to end 2026-08-04 with requests, httpx and curl).
  - **`curl -X POST -L` gotcha**: an explicit `-X` sticks across redirects, so curl re-POSTs to `/people/{vanId}` — the *update* endpoint — with no body, and IIS answers `411 Length Required` (HTML). Plain `-d` (which already implies POST) lets curl 8.7.1 downgrade to GET and return the 200. Same trap in any client configured to preserve the method on 302.
- **Person field inventory** observed live via `GET /people/{vanId}` (~70 fields, most null on a minimal record — see the `Person` schema). `dateAcquired` is `0001-01-01T00:00:00Z` (dotnet default) for API-created records.
- **`POST /surveyQuestions`** requires all `*OtherLanguage` fields, `scope`, and a valid `type`; `Candidate`/`Issue`/`Volunteer`/`GOTV` all rejected as invalid type in the demo committee — valid values still unknown (blocker for seeding sandbox list data).
- **`GET /people/{vanId}`**: unknown, `0`, and negative ids all return the JSON `NOT_FOUND` envelope (contrast `/people/find`, which 500s on `vanId: 0`). A non-integer path segment misses the route and returns an **IIS HTML 404 page** (`text/html`) — documented as a second 404 media type in the spec.
- **`$expand` on `GET /people/{vanId}`**: 23 valid values enumerated by the API's own `INVALID_PARAMETER` hint (official docs list only 10): contributionHistory, addresses, phones, emails, codes, customFields, externalIds, preferences, recordedAddresses, reportedDemographics, suppressions, cases, customProperties, districts, electionRecords, membershipStatuses, notes, organizationRoles, scores, disclosureFieldValues, primaryContact, pollingLocation, biography. Expanded collections come back as `[]` when empty vs `null` when not expanded. Two of the 23 are still refused in My Campaign mode — probed 2026-08-04 by requesting each section in turn against `102714507`: `scores` → `403 {"code":"FORBIDDEN","text":"May not request 'scores'"}` and `pollingLocation` → `400 BAD_REQUEST` "May not request 'pollingLocation' in this context". Same over-promising hint as the search endpoint, but with **inconsistent status codes for the same kind of refusal**. Surfaced only once `$expand` was modelled as an enum of the valid sections (the fuzzer had never guessed a valid one before); spec'd as a shared `Forbidden` 403 response on `getPerson`.
- **`$expand` on `GET /people` (search)** is a much smaller set — the hint names only Addresses, Emails, Phones, Districts, PollingLocation — and the hint over-promises: `pollingLocation` is refused with a differently-shaped error, `400 {"code":"BAD_REQUEST","text":"May not request 'pollingLocation' in this context"}` (no `properties`, no `hint`), alone or combined with other sections. Probed 2026-08-04 in My Campaign mode only; it may be expandable in My Voters. The usable set is addresses, emails, phones, districts, all captured as response examples on the operation.
- **Person write endpoints** — none are in the docs' page index (`llms.txt` lists only `/people/find`); paths come from van-cli `people.ts`, bodies from the docs' "Match Candidate" common model. All three take the same body (`PersonInput`) and answer with the same `{vanId, status}` stub:
  - `POST /people/create` → `201 {"status":"Stored"}`. **No de-duplication**: an exact copy of an existing person's name + email creates a second record. `POST /people/findOrCreate` → `302 Matched` on a hit, `201 {"status":"UnmatchedStored"}` otherwise — it never 404s. `POST /people/{vanId}` → `302 Matched`; unknown/0/negative id → `404`.
  - **Everything is optional.** `POST /people/create` and `/people/findOrCreate` both accept `{}` and create a blank, nameless record — van-cli enforces firstName/lastName client-side, the API does not.
  - A body `vanId` that exists short-circuits everything: even `create` returns `302 Matched` and writes nothing. `vanId: 0` is silently ignored here (contrast `/people/find`, which 500s on it).
  - **Update is a merge, not a replace**: omitted fields keep their values, and `emails`/`phones`/`addresses` are *appended* — sending the same email twice stores it twice. There is no way to remove a contact method through this endpoint.
  - **`isPreferred` behaves differently for emails and phones.** A newly added email takes the preferred flag off the previous one automatically, even when several already exist and none of the writes set the flag; a newly added phone does not — seven phones written with no flag all read back `isPreferred: false`.
  - **Validation is uneven.** Enforced: `sex` (M/F, case-insensitive), `contactMethodPreferenceCode` (P/E/M/F/S), `electionType` (10 codes), `dateOfBirth` century, every max length (`firstName` 75, `title` 10, `party` 1, `cycle` exactly 4, `ext` 6), email format, phone number validity. **Not** enforced: `contactMode` (any value → stored as `Person`), `party` (any single character stored verbatim), address `type`, and an unparseable `dateOfBirth` (silently dropped to null rather than 400).
  - Context-scoped lookups reject cleanly with 400 in the demo committee — `customFieldValues.customFieldId`, `selfReportedRaces.reportedRaceId`, `identifiers.type` — so those write paths could only be observed failing. **BUG**: `identifiers` with the *valid* type `dwid` returns 500, not 400.
- **Contact sub-models** (`PersonEmail`, `PersonPhone`, `PersonAddress`, `PhoneIsCellStatus`) and their write-side twins (`PersonEmailInput`, `PersonPhoneInput`, `PersonAddressInput`) — split because the same field name carries different value domains in each direction (`phoneType: "C"` in, `"Personal"` out), which `readOnly` cannot express. Enums pinned down by writing every documented code through `POST /people/findOrCreate` and reading it back with `$expand`:
  - **Emails read back the codes they were written with** (`type` P/W/O, `subscriptionStatus` U/N/S). **Phones do not**: writes take single letters, reads return words. `phoneType` H/C/M/P → `Personal`, W/F → `Work`, O → `Other`; `deviceType` C/L/U/F/V → `Cell`/`Landline`/`Unknown`/`Fax`/`VoIP`; `phoneOptInStatus` I/U/O → `Opt-In`/`Unknown`/`Opt-Out`. Writing `phoneType: "C"` also sets `deviceType: "Cell"`, and `"F"` sets `"Fax"`.
  - `isSubscribed` is a lossy projection of `subscriptionStatus`: `S`→true, `U`→false, `N`→**null** (neutral has no boolean form). Writing `isSubscribed: false` stores `U`, which is terminal.
  - Undocumented **read-only phone fields**: `dateCreated`, `isBest`, `isDisclosure`, `smsOptInStatus` (mirrors `phoneOptInStatus`), `isCellPhoneExchange`, `tags`.
  - **Nulls are rarer than they look** (probed 2026-08-04 with minimal writes, explicit-null writes, and a blank `{}` person): the server substitutes defaults instead of storing null. Phones: only `ext`, `isCellStatus` and `tags` can be null — everything else defaults (`phoneType` `Personal`, `deviceType` `Unknown`, opt-in statuses `Unknown`, `dialingPrefix`/`countryCode` from the context). Emails: `type` defaults `P` and `isPreferred`/`dateCreated` are always set, but `subscriptionStatus` **can** read back null (four older non-preferred addresses on `102714580` have it — rule unknown; a fresh write without it defaults `S`), and `isSubscribed` is null both there and for the `N` mapping. Addresses: `city`/`stateOrProvince` are inferred from the zip, a missing zip reads back `""` (empty string, not null), and an address too sparse or unresolvable to standardize (street line only, or an unknown zip) is silently not stored/surfaced at all. Person: even a blank record has `contactSource`, `contactMode`, `formalSalutation` (`"Friend"`), `formalEnvelopeName` (`"Our Friend At"`), `isDeceased`, `dateCreated` and `dateAcquired` — but plain `salutation`/`envelopeName` *are* null on it. Spec tightened accordingly.
  - Undocumented **read-only address fields**: `unitNo`, `geoLocation`, `preview`, `streetAddress`, `displayMode` (only `"Standardized"` seen).
  - `$expand=addresses` returns **at most two entries** — best known Home (typed `Voting`) and best known Mailing — with the *same* `addressId` repeated when one address fills both roles. Addresses written as `Work` or `Custom` are stored but never surfaced by this expansion. The written `type` is not validated at all: `"Q"` is accepted with 201 and filed as the home address.
  - `isCellStatus` is validated on write (`statusId` 1–4, `statusName` `Verified Cell`/`Likely Cell`/`Likely Not a Cell`/`Not a Cell`, and a mismatched id/name pair is accepted) but always reads back `null`. `GET /phones/isCellStatuses` exists and returns a bare array — empty in the sandbox.
- **`GET /people` (search)** — absent from the official docs' page index; path and parameter list from van-cli `people.ts`. Returns full `Person` records in the standard pagination envelope (`count` = total matches, `nextPageLink` an absolute URL with an explicit `:443` port). Probed 2026-08-04:
  - Requires at least one **search** parameter — firstName, lastName, middleName, streetAddress, city, stateOrProvince, zipOrPostalCode, phoneNumber, email, commonName, officialName — otherwise 400 `INVALID_PARAMETER` "This endpoint requires at least one search parameter to be set.", even when filter/paging parameters are present.
  - Text criteria are case-insensitive **prefix** matches (`lastName=Testpers` hits "Testperson"; `estperson` does not). `email` matches against all of a person's addresses, not just the preferred one. Non-ASCII values just return 0 results — no `/surveyQuestions`-style 500.
  - `phoneNumber` is validated: non-numerics stripped, then must match the NANP regex `^1?[2-9][0-8]\d[2-9]\d{6,6}$` (from the API's own hint), else 400.
  - `contactMode` filters to Person/Organization records but does **not** count as a search parameter, and an invalid value is silently ignored. `employer`, `occupation` and `dateOfBirth` are accepted but do nothing at all (even `dateOfBirth=notadate` passes silently).
  - `$top` default 50 / max 200 — van-cli caps people search at 50 client-side, but that limit is not server-side. `$skip` must be ≥ 0. `$orderby` accepts only `Name` (and `Name desc`). For `$expand` see the dedicated bullet above (usable set: addresses, emails, phones, districts).
  - **Person schema discoveries** (fuzzer-found on this endpoint, apply to the shared schema): search results return `formalSalutation`/`formalEnvelopeName` as **null** even for records where get-by-id derives them; and there is a `partialDateOfBirth` `{month, day, year}` field on both search and get-by-id responses — missed by earlier field inventories because it is null whenever `dateOfBirth` is null.
- **BUG (request escapes the API)**: a path segment containing `<`, `>`, `*` or `&` (e.g. `GET /people/1%3E`) trips ASP.NET request validation before routing, and the request is handed to the **VAN web application** instead: `302` → `Location: /Error.aspx?EID={code}` with an HTML "Object moved" body; following it gives `200 text/html` (the "Oops...something went wrong" page). So a JSON-only endpoint can answer `200` with HTML. Neighbouring characters take three other paths: `:` and `"` → IIS HTML 404, `\` → JSON 404 envelope, plain non-integer (`abc`) → IIS HTML 404. Found by the stateful fuzz phase; spec'd as the shared `RequestValidationRedirect` response plus a `text/html` variant on the 200s.
- **Two more ways a request dies before reaching the API**, both answering `400 text/html` where the spec previously only allowed JSON: a percent-encoded control character in the path (`%7F`) gets IIS's `Bad Request - Invalid URL` page (`charset=us-ascii`, 324 bytes), and `%00` gets Cloudflare's `400 Bad Request` page. Found 2026-08-04 by the fuzzer's coverage phase on `POST /people/{vanId}/notes`, then reproduced on `GET /people/{vanId}` — path-position independent and endpoint independent, so `text/html` is now a documented media type on the shared `BadRequest` response rather than on the notes operations. Completes the picture alongside the other path-character routes: `%25` → the ASP.NET 302 to `/Error.aspx`, `<>*&` → the same 302, `:`/`"`/non-integer → IIS HTML 404, `\` → JSON 404.
- **Transient 502**: one fuzz run (2026-08-04) caught a bare nginx `502 Bad Gateway` HTML page from `POST /people/find` (payload had `dateOfBirth: [null, null]`); the identical request returned 400 on three immediate retries. Treated as infrastructure flakiness, not API behavior — deliberately not spec'd, but it can turn a `make fuzz` run red once in a while.
- **Query strings over ~2048 characters never reach the API**: IIS rejects the request and answers its own HTML `404 - File or directory not found` page, so a 404 on a search endpoint can mean "URL too long" rather than "nothing found" — only the content type tells them apart. Bisected on `GET /surveyQuestions?statuses=AAA…` (2026-08-04): 2000 characters still reach the API (which 500s on the garbage value), 2100 return the IIS 404; reproduced on `GET /people` with a long `lastName`, so it is infrastructure-wide rather than endpoint-specific. Found by the fuzzer once `statuses` became an enum array. Spec'd as the shared `QueryStringTooLong` 404 on `GET /people` and `GET /surveyQuestions`; on `GET /people/{vanId}` it is folded into `PersonNotFound`, which already documents a `text/html` 404.
- **BUG (stack-trace disclosure)**: a query-string key containing `[` (e.g. `?a[=1`) crashes ASP.NET's jQuery-syntax key parsing → 500 with a raw `{"message","exceptionMessage","exceptionType","stackTrace"}` payload — full server stack trace leaked, and a different body shape than the standard envelope. Found by the stateful fuzz phase on `GET /people/{vanId}`; the shared 500 response now accepts both shapes (`AspNetError` schema).
- **Person notes** (probed 2026-08-04 on `102714507`; docs pages `peoplevanidnotes`, `peoplevanidnotes-1`, `peoplevanidnotesnoteid`, `notescategories` — none of them in `llms.txt`, all reachable by slug — plus van-cli `notes.ts`):
  - `GET /people/{vanId}/notes` → standard envelope. `$top` default **25, max 50** (a different profile from `/surveyQuestions`' 50/200), `$skip` ≥ 0, and two extra parameters that are **validated but inert**: `$orderby` (only `CreatedDate`/`IsPinned`, case-insensitive, `desc` accepted) and `$expand` (only `noteDetails`). Every accepted `$orderby` value, `desc` included, returns the same order — oldest `createdDate` first, ties by descending `noteId` — and `$expand=noteDetails` returns a byte-identical body.
  - `POST /people/{vanId}/notes` → **204, empty body, no `Location`** — the new `noteId` is unobtainable without re-listing. Non-idempotent. Unknown fields ignored; unknown `vanId` → 404.
  - **View-restricted notes are write-only through the API**: a note created with `isViewRestricted: true` returns 204 but is then missing from `GET /people/{vanId}/notes` *and* from `$expand=notes`, and is not counted in `count`. So every note the API returns has `isViewRestricted: false`, and a restricted note can never be read, updated or even confirmed.
  - `isPinned` is accepted on create and silently ignored (always reads back false). `contactHistory` on create does record a Contact History entry, but the note's own `contactHistory` is **always null** in every response, `$expand=noteDetails` included — the populated shape was never observed, so it is left unmodelled.
  - **BUG (500 band under the stated limit)**: `text` of 7001–8000 characters returns 500 without saving, while 8001 is correctly rejected with 400 `'text' must be between 0 and 8000 characters`. Bisected: 7000 → 204, 7001 → 500. Spec'd as `maxLength: 7000` to keep the fuzzer out of the band.
  - **BUG (500 on the API's own value)**: `category.noteCategoryId: 0` → 500, even though 0 is exactly what the API reports for an uncategorized note. An unknown-but-nonzero id gets a proper 400. Spec'd as `minimum: 1`. Same shape of bug on `contactHistory.resultCodeId`: an id outside `/canvassResponses/resultCodes` → 500, while a bad `contactTypeId` → 400.
  - **Inconsistent `category` rendering**: an uncategorized note is `{"noteCategoryId": 0, "name": null}` from `GET /people/{vanId}/notes` but plain `null` from `GET /people/{vanId}?$expand=notes` — same note, same fields otherwise, different order. `Person.notes` is now typed against the shared `Note` schema with this called out.
  - `GET`, `PUT` and `DELETE /people/{vanId}/notes/{noteId}` all return `403 FORBIDDEN` "Access to this action is restricted" for this key, before the note is even looked up (a nonexistent `noteId` 403s too) — same restriction as `DELETE /people/{vanId}`, spec'd as the shared `ActionRestricted` response. `PUT` is in the spec with its docs-derived body and 204 (`x-provenance` marks the success path unobserved); `GET`/`DELETE` by id are left out, since neither the docs nor van-cli pin down their success shapes.
  - `GET /notes/categories` → **bare array, no envelope** (like `/canvassResponses/contactTypes`); query parameters ignored. The demo committee has none opted in, so only `[]` was observed and the item shape (`noteCategoryId`, `name`, `assignableTypes`) comes from the docs. Categories must be opted in to the committee in the VAN UI to appear here at all, which also blocks probing any valid `category` value on create.
- **Stateful testing**: the spec now defines OpenAPI `links` from `POST /people/find` (302 and redirect-followed 200) to `GET /people/{vanId}` via `$response.body#/vanId`; schemathesis's stateful phase picks them up automatically in `make fuzz`. The write endpoints add links from their 201/302 responses to `getPerson`, and `GET /people` links its 200 to `getPerson` via `$response.body#/items/0/vanId`, and `POST /people/{vanId}/notes` links its bodyless 204 to `listPersonNotes` via `$request.path.vanId` (the only way to find the note it just made), taking the total to 31 (22 of them inferred by schemathesis itself) across ~75–95 scenarios per run — but only **2 are actually covered** per run. The 200-response links can't be exercised by schemathesis's raw (non-redirect-following) client, and the rest simply aren't reached at this example budget; worth revisiting if stateful coverage matters more than wall-clock.
- Sandbox committee: "Ship Creek Group (APIKR-2320)", Demo State, 30M req/month quota, rate limits High=200/s, Medium=15/s.
- Test data created in sandbox: person `vanId 102714507` (Ada Testperson, ada.testperson@example.com); contact-model probes `102714558`–`102714568` (Grace Testperson plus Badaddr/Multi Phonetest/Device Testtwo/Addr Testthree/Cell Statustest &c., each seeded with the phone, email or address variants named above); write-endpoint probes `102714580`–`102714595`; nullability probes `102715741`–`102715746`, `102715807` and `102716327` (Nulltest Minimal/Explicit/Zippy/Streetonly/Cityonly/Badzip/Sinkmail, 2026-08-04). **`102714580` (Cre Atetest) is the designated update target** — it is the `vanId` example on `POST /people/{vanId}`, so `make fuzz` mutates it every run; keep it out of any example that needs stable field values. The fuzzer also creates a few hundred throwaway records per run via `POST /people/create` and `/people/findOrCreate`.
- Notes created in sandbox on `102714507` during the notes research (2026-08-04): `noteId` 184579–184585 plus the text-length bisection notes, one of them (`184580`) view-restricted and therefore invisible to the API. `102714507` is the `vanId` example on both notes operations, so `make fuzz` adds more notes to it every run — its note list is not stable test data.
